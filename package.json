{
  "name": "check-tarefas-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:api": "cd server && npm install && npx prisma generate && npm run build",
    "build:web": "cd web && npm install && npm run build",
    "render:build": "npm run build:api && npm run build:web"
  }
}